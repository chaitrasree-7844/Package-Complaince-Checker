import { useState } from "react";
import { Bell, LockKeyhole, LogOut, Moon, Save, ShieldCheck, Sun, UserRound } from "lucide-react";
import { useNavigate } from "react-router-dom";
import { toast } from "sonner";
import { getCurrentUser, logoutUser, updateUser } from "@/services/mockAuthApi";
import type { AccountType } from "@/types/compliance";
import { setTheme, useTheme } from "@/lib/theme";

export default function Settings() {
  const navigate = useNavigate();
  const user = getCurrentUser();
  const [fullName, setFullName] = useState(user?.fullName ?? "");
  const [email, setEmail] = useState(user?.email ?? "");
  const [accountType, setAccountType] = useState<AccountType>(user?.accountType ?? "normal");
  const [notifications, setNotifications] = useState(true);
  const theme = useTheme();
  const save = async () => { if (!user) return; try { await updateUser(user.id, { fullName, email, accountType }); toast.success("Settings saved"); } catch (error) { toast.error(error instanceof Error ? error.message : "Unable to save settings"); } };
  const logout = () => { logoutUser(); toast.success("Logged out"); navigate("/login", { replace: true }); };
  return <div data-testid="settings-page" className="mx-auto max-w-4xl space-y-7"><section><p data-testid="settings-eyebrow" className="text-sm font-semibold text-blue-600">Workspace preferences</p><h1 data-testid="settings-title" className="mt-2 font-heading text-3xl font-semibold tracking-tight text-slate-950">Settings</h1><p data-testid="settings-description" className="mt-2 text-sm text-slate-500">Manage your prototype account and display preferences.</p></section><section data-testid="settings-account-card" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_25px_rgba(15,23,42,0.04)] sm:p-7"><div className="flex items-center gap-3 border-b border-slate-100 pb-5"><span className="rounded-xl bg-blue-50 p-2.5 text-blue-600"><UserRound className="size-5" /></span><div><h2 data-testid="settings-account-title" className="font-heading text-lg font-semibold">Account</h2><p data-testid="settings-account-description" className="mt-1 text-sm text-slate-500">Your profile details used across the workspace.</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><div><label data-testid="settings-full-name-label" htmlFor="settings-name" className="mb-2 block text-sm font-semibold text-slate-700">Full name</label><input data-testid="settings-full-name-input" id="settings-name" value={fullName} onChange={(event) => setFullName(event.target.value)} className="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10" /></div><div><label data-testid="settings-email-label" htmlFor="settings-email" className="mb-2 block text-sm font-semibold text-slate-700">Email</label><input data-testid="settings-email-input" id="settings-email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} className="h-11 w-full rounded-xl border border-slate-200 px-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10" /></div><div><label data-testid="settings-account-type-label" htmlFor="settings-type" className="mb-2 block text-sm font-semibold text-slate-700">Account type</label><select data-testid="settings-account-type-select" id="settings-type" value={accountType} onChange={(event) => setAccountType(event.target.value as AccountType)} className="h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-sm outline-none focus:border-blue-500 focus:ring-4 focus:ring-blue-500/10"><option value="normal">Normal User</option><option value="authorized">Authorized User</option></select></div></div><button data-testid="settings-save-button" onClick={save} className="mt-6 inline-flex h-10 items-center gap-2 rounded-xl bg-blue-600 px-4 text-sm font-semibold text-white hover:bg-blue-700"><Save className="size-4" />Save changes</button></section><section data-testid="settings-preferences-card" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_25px_rgba(15,23,42,0.04)] sm:p-7"><div className="flex items-center gap-3 border-b border-slate-100 pb-5"><span className="rounded-xl bg-amber-50 p-2.5 text-amber-600"><Bell className="size-5" /></span><div><h2 data-testid="settings-preferences-title" className="font-heading text-lg font-semibold">Preferences</h2><p data-testid="settings-preferences-description" className="mt-1 text-sm text-slate-500">Choose how this prototype behaves.</p></div></div><div className="mt-5 divide-y divide-slate-100"><label data-testid="settings-notifications-row" className="flex items-center justify-between gap-4 py-4"><span><span className="block text-sm font-semibold text-slate-700">Notifications</span><span className="mt-1 block text-xs text-slate-500">Show completion and export feedback</span></span><input data-testid="settings-notifications-toggle" type="checkbox" checked={notifications} onChange={(event) => setNotifications(event.target.checked)} className="size-4 accent-blue-600" /></label><div data-testid="settings-theme-row" className="flex items-center justify-between gap-4 py-4"><span><span className="block text-sm font-semibold text-slate-700">Theme</span><span data-testid="settings-theme-description" className="mt-1 block text-xs text-slate-500">Uses your device preference initially, then remembers your selection</span></span><button data-testid="settings-theme-toggle" type="button" aria-label={`Switch to ${theme === "dark" ? "light" : "dark"} mode`} aria-pressed={theme === "dark"} onClick={() => setTheme(theme === "dark" ? "light" : "dark")} className="inline-flex h-10 min-w-28 items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-700 hover:bg-slate-50">{theme === "dark" ? <><Sun className="size-4" aria-hidden="true" />Light mode</> : <><Moon className="size-4" aria-hidden="true" />Dark mode</>}</button></div></div></section><section data-testid="settings-security-card" className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_25px_rgba(15,23,42,0.04)] sm:p-7"><div className="flex flex-wrap items-center justify-between gap-4"><div className="flex items-center gap-3"><span className="rounded-xl bg-slate-100 p-2.5 text-slate-600"><LockKeyhole className="size-5" /></span><div><h2 data-testid="settings-security-title" className="font-heading text-lg font-semibold">Security</h2><p data-testid="settings-security-description" className="mt-1 text-sm text-slate-500">Passwords are never shown in settings.</p></div></div><button data-testid="settings-change-password-button" onClick={() => toast.info("Password changes will be connected to the backend authentication service.")} className="rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50">Change password</button></div><button data-testid="settings-logout-button" onClick={logout} className="mt-6 inline-flex items-center gap-2 rounded-xl border border-rose-200 px-4 py-2.5 text-sm font-semibold text-rose-700 hover:bg-rose-50"><LogOut className="size-4" />Log out</button></section><p data-testid="settings-prototype-note" className="flex items-center justify-center gap-2 text-xs text-slate-400"><ShieldCheck className="size-3.5" /> Local prototype storage is ready to be replaced by the future backend.</p></div>;
}